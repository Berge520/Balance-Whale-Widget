# 统一行尾规范：入库一律 LF，检出到工作区也保持 LF。
#
# 为什么需要它：此前仓库没有本文件，行尾完全依赖各人本地的 core.autocrlf
# （Windows 上常见 autocrlf=true）。同一份文件在不同机器 / 不同工具写入时，
# 工作区可能落成 LF 或 CRLF，Git 于是把文件标成 modified，但规范化后内容
# 完全一致 —— git diff 为空。这类「假 modified」会污染 git status，
# 也让提交时容易误带噪声。声明成仓库级规则后，所有协作者行为一致。
#
# 用 eol=lf 而不是裸 text=auto：后者仍受各人 core.autocrlf 影响，
# 消除不掉上述假 modified。源码与文档统一 LF，VS Code 等编辑器按内容识别，
# 不影响本地开发。
* text=auto eol=lf

# 无扩展名的文本文件（GitHub 不识别时不会自动规范化）
LICENSE      text eol=lf
LICENSE-MIT  text eol=lf

# 图片 / 音频等二进制：禁止任何行尾转换与文本差异处理，避免被误判为文本损坏
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.bmp   binary
*.ico   binary
*.svg   text eol=lf
*.mp3   binary
*.wav   binary
*.m4a   binary
*.ogg   binary
*.zip   binary
*.upx   binary
*.exe   binary
*.ttf   binary
*.woff  binary
*.woff2 binary
